# Proxy-Server
A high-performance HTTP caching proxy built with Spring Boot and Redis. This proxy server forwards client requests to an origin server, caches responses in Redis, and serves subsequent requests directly from cache for improved performance and reduced load on the origin.
